{
  "name": "bamboohr-cli",
  "version": "1.0.0",
  "description": "CLI for BambooHR API - designed for agentic AI usage",
  "type": "module",
  "bin": {
    "bamboohr": "./bin/bamboohr.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node bin/bamboohr.js",
    "install-cli": "npm install && npm run build && npm link",
    "uninstall-cli": "npm unlink -g bamboohr-cli"
  },
  "keywords": [
    "bamboohr",
    "hr",
    "cli",
    "api"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^13.1.0",
    "undici": "^8.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0"
  }
}
